# Local Skills Index

## 1) iaa-server-context-recovery

Path:

- `.idea/skills/iaa-server-context-recovery/SKILL.md`

Use when:

- reopening workspace and needing fast project context recovery
- confirming architecture/config/API/runbook before making changes

## 2) iaa-svr-game-debugval

Path:

- `.idea/skills/iaa-svr-game-debugval/SKILL.md`

Use when:

- maintaining/extending `GET /debug_val` and `POST /debug_val_inc`
- touching player Mongo schema/index/update semantics
- validating gateway -> game identity flow (`X-OpenID`)
